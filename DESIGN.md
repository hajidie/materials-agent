# Materials Agent UI

中文聊天式材料智能助手。用户发送消息、上传附件，由 Agent 完成工作，从消息查看结果。
保留墨绿/灰绿、系统中文字体、左侧对话列表与聊天工作区；不建设科研软件或 ML 管理后台。

## Canonical UI Map

- 颜色、布局、断点、focus-visible：frontend/src/styles.css。
- 消息与结果时间线：App/AgentRunCard；只展示语义结果，不展示运行 ID、内部状态码或原始 JSON。
- 统一输入与附件：ChatComposer/useAgentRuns；CSV、EBSD PNG/JPEG 共用“＋”，每条消息一个文件。
- 消息附件/结果查看：ArtifactViewer/ResultPresentation；原生 dialog、Escape 关闭、返回触发按钮焦点。
- 独立终态观察：useChatArtifacts；与 Viewer 无依赖，显式 POST 去重追加结果，GET 保持只读。
- 对话列表：ConversationSidebar；单行等高标题、省略长文本、删除按钮始终可达。
- 删除与焦点恢复：DeleteConversationDialog；未确认删除保留对话与 fence。
- 受控图片内容、文件下载：消息 Artifact API；不接受任意远程 URL。

## 交互约束

唯一核心入口是聊天。无资源中心、Dataset/Training/Model 分类列表、训练配置表单或开发诊断面板。
资源由消息关联，点击附件/结果卡片打开详情。查看/关闭/下载不更改选择、草稿、工具输入或 Agent 状态。
当前模型输出是纯文本，不使用 v-html；附件名称、数据列与指标同样按纯文本渲染。

输入草稿和不确定上传按对话/补参目标隔离。确认提交后才清空；网络不确定保留原请求与幂等键，
提供“检查原提交”“核查原上传”，不重复上传文件或重派发。移除附件只解除草稿关联。
Enter 发送、Shift+Enter 换行、IME composition 不发送。文件校验错误可见，上传中禁用提交。

空态输入区居中；有消息后输入区位于记录下方。启动与运行进度只占一个临时区域，避免技术状态刷屏。
持久化启动回答不可改写，终态通过独立助手消息追加一次；性能指标保留真实值和单位，未知单位明确说明。
不将训练提交当作训练成功，不虚构置信区间；EBSD 保留 Inconel 625 适用范围与实验性限制。

详情有加载、失败和重试状态；旧请求不能覆盖切换后的附件。宽屏居中受限宽度，窄屏对话框适应视口；
数据表在自身容器横向滚动，不导致整页溢出。所有按钮可键盘操作，状态区域具有可访问名称。

## 验证约定

组件测试覆盖统一附件、未知上传恢复、Viewer 只读、不可变消息与去重、删除 fence；运行 TypeScript 与构建。
实际浏览器检查聊天、附件/结果详情、键盘焦点、错误恢复及窄屏；确定性模型 UI 验证与真实 Provider 验证分别报告。
新增工具不得要求新增前端管理页面，只增加现有结果事实投影与消息附件合同。
